%%-*- mode: erlang -*-

{deps, [
    {parselib, ".*",
        {git, "https://github.com/kostyushkin/parselib.git", {branch, "master"}}
    }
]}.

{eunit_opts, [
    verbose, 
    {report, {eunit_surefire, [{dir, "."}]}}
]}.

{clean_files, ["ebin", "deps"]}.
